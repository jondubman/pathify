import * as React from 'react';

import {
  StyleSheet,
  TouchableHighlight,
  View,
} from 'react-native';

import { MenuProps } from 'components/containers/MenuContainer';
import constants from 'lib/constants';

const Styles = StyleSheet.create({
})

const Menu = (props: MenuProps) => (
  <View />
)

export default Menu;
