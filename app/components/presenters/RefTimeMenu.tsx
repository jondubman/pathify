import * as React from 'react';

import {
  StyleSheet,
  View,
} from 'react-native';

import { RefTimeMenuProps } from 'containers/RefTimeMenuContainer';

const Styles = StyleSheet.create({
})

const RefTimeMenu = (props: RefTimeMenuProps) => (
  <View />
)

export default RefTimeMenu;
