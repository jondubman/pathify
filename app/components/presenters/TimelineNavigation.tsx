// TODO this component is currently unused

import * as React from 'react';

import {
  StyleSheet,
  View,
} from 'react-native';

import { TimelineNavigationProps } from 'containers/TimelineNavigationContainer';

const Styles = StyleSheet.create({
})

const TimelineNavigation = (props: TimelineNavigationProps) => (
  <View />
)

export default TimelineNavigation;
